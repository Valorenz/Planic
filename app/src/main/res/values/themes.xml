<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Planic" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/btn_color</item>
        <item name="colorPrimaryVariant">@color/btn_color</item>

        <item name="android:windowBackground">@color/bg_color</item>
    </style>

    <style name="Theme.Planic" parent="Base.Theme.Planic" />
</resources>